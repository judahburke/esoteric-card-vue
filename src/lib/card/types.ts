type CutOptions = {
  cutMinimum: number;
};

type DealOptions = {
  cardCount: number;
  isDealerFirst: boolean;
};

type ShuffleOptions = {
  shuffleCount: number;
};

export type { CutOptions, DealOptions, ShuffleOptions };
